/**
 * Opt-in pagination dots, shipped as the tree-shakeable `lightslide/pagination` entry.
 * Import and pass to `<LightSlide pagination={<Pagination />}>`; bundles that don't never
 * pay for the dots or their styles.
 */
export {Pagination} from './Pagination';
export type {PaginationProps} from './Pagination.types';
