/**
 * Opt-in wheel/trackpad gestures, shipped as the tree-shakeable `lightslide/wheel` entry.
 * Import it and pass to `<LightSlide wheel={<Wheel />}>`; bundles that don't never pay for it.
 */
export type {WheelProps} from './Wheel';
export {Wheel} from './Wheel';
