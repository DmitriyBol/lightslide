/**
 * Opt-in momentum (free-scrolling) drag physics, shipped as the tree-shakeable
 * `lightslide/free` entry. Import it and pass to `<LightSlide free={<FreeScroll />}>`
 * (or `<FreeScroll snap />` for free-snap); bundles that don't never pay for it.
 */
export type {FreeScrollProps} from './FreeScroll';
export {FreeScroll} from './FreeScroll';
