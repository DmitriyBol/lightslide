/**
 * Opt-in continuous ticker, shipped as the tree-shakeable `lightslide/flow` entry. Import it
 * and pass to `<LightSlide flow={<Flow />}>`; bundles that don't never pay for it.
 */
export type {FlowProps} from './Flow';
export {Flow} from './Flow';
